import { IfActivityAllowedDirective } from './if-activity-allowed.directive';

describe('IfActivityAllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new IfActivityAllowedDirective();
    expect(directive).toBeTruthy();
  });
});
