import { AxCanDirective } from './ax-can.directive';

describe('AxCanDirective', () => {
  it('should create an instance', () => {
    const directive = new AxCanDirective();
    expect(directive).toBeTruthy();
  });
});
